# =========================================================================
# Ziza Platform — local environment template
# Copy to .env and fill in. NEVER commit .env.
# =========================================================================

# Backend
ENVIRONMENT=dev
CORS_ORIGINS_RAW=http://localhost:3001,http://localhost:3002,http://localhost:3003
PORT=8000

# Auth DEV (Sprint 2) — change AUTH_DEV_SECRET before sharing this env
AUTH_DEV_SECRET=dev-secret-change-me
AUTH_DEV_PASSWORD=ziza2024
AUTH_DEV_TOKEN_TTL_HOURS=24

# Auth PROD / Firebase (Sprint 3)
FIREBASE_PROJECT_ID=

# Frontends (build-time)
VITE_API_URL=http://localhost:8000

# GCP (only needed locally if you run scripts/deploy/* manually)
GCP_PROJECT_ID=
GCP_REGION=us-central1
AR_REPO=ziza
